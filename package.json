{
  "dependencies": {
    "cypress": "^15.6.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^4.0.2",
    "dotenv": "^17.2.3"
  },
  "scripts": {
  "cypress:run": "cypress run",
  "cypress:report": "cypress run && mochawesome-merge cypress/reports/*.json | mochawesome-report-generator"
}

}
