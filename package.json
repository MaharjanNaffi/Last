{
  "dependencies": {
    "cypress": "^15.6.0"
  },
  "devDependencies": {
    "cypress-mochawesome-reporter": "^4.0.2",
    "mochawesome-merge": "^4.3.0",
    "mochawesome-report-generator": "^6.2.0",
    "dotenv": "^17.2.3"
  },
  "scripts": {
    "cypress:run": "cypress run",
    "cypress:report": "cypress run && mochawesome-merge cypress/reports/*.json -o cypress/reports/merged-report.json && npx mochawesome-report-generator cypress/reports/merged-report.json -o cypress/reports -f merged-report"
  }


}
